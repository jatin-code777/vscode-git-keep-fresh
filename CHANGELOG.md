# Changelog

## 0.1.0

- Initial release
- Auto-pulls configured branches at regular intervals
- Fast-forward updates for non-checked-out branches (no disruptions!)
- Optional active branch pull with `--ff-only`
- Status bar showing sync status and last pull time
- Commands: Start, Stop, Pull Now, Add Branch, Remove Branch
- Configurable interval and branch list
